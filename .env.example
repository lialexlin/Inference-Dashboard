# Inference Dashboard — local secrets template.
# Copy to .env and fill in. Never commit .env.

# Finnhub (free tier — sign up at finnhub.io/register).
# Optional: only used if/when finnhub source is wired in.
FINNHUB_API_KEY=

# S&P Capital IQ via Snowflake. Required for `jobs/sources/capital_iq.py`.
# Skill reference: ../Exponential-Growth/.claude/skills/s&p/SKILL.md
SF_ACCOUNT=
SF_USER=
SF_PASS=
